export type RouteKeys = "aprova" | "pre-qualificacao" | "recompra";

export const routes: Record<RouteKeys, string> = {
  "pre-qualificacao": "/pre-qualificacao",
  aprova: "/aprova",
  recompra: "/recompra",
};
